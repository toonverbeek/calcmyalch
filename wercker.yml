box: fedora 
dev:
  steps:
    - internal/watch:
        code: go build && ./calchmyalch
        reload: true
build:
    steps:
        - script:
            name: memory stresstest install
            code: echo HOI
