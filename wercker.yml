box: google/golang
dev:
  steps:
    - internal/watch:
        code: go build && ./calchmyalch
        reload: true
build:
    steps:
        - script:
            name: ping github 
            code: for i in {1..10}; do curl github.com; sleep 1; done 
