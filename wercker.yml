box: fedora 
dev:
  steps:
    - internal/watch:
        code: go build && ./calchmyalch
        reload: true
build:
    steps:
        - script:
            name: memory stresstest install
            code: yum -y install stress && yum clean all
        - script:
            name: run test
            code: stress --vm 1 --vm-bytes 128M --vm-hang 0
