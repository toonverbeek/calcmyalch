box: 
  id: phusion/baseimage
  repository: 52.0.31.125
dev:
  steps:
    - internal/shell
build:
    steps:
        - script:
            name: memory stresstest install
            code: |
              echo HOI-$WERCKER_BUILD_ID >> $WERCKER_CACHE_DIR/hoi.txt
              touch $WERCKER_CACHE_DIR/test-$WERCKER_BUILD_ID
              ls -la $WERCKER_CACHE_DIR
              cat $WERCKER_CACHE_DIR/hoi.txt

