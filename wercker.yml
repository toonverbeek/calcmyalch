box: google/golang
build:
    steps:
        - script:
            name: env && export
            code: echo $TEST_PRIVATE
                  echo $VISIBLE
                  echo $HIDDEN 
                  
