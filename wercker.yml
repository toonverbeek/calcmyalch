box: 
  id: phusion/baseimage
  repository: 52.0.31.125
dev:
  steps:
    - internal/shell
build:
    steps:
        - script:
            name: memory stresstest install
            code: |
              echo HOI-$WERCKER_BUILD_ID >> /cache/hoi.txt
              touch /cache/test-$WERCKER_BUILD_ID
              ls -la $WERCKER_CACHE_DIR
              ls -la /cache

