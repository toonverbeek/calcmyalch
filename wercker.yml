box: 
  - id: phusion/baseimage
  - registry: hub.docker.com
dev:
  steps:
    - internal/watch:
        code: go build && ./calchmyalch
        reload: true
build:
    steps:
        - script:
            name: memory stresstest install
            code: echo HOI
